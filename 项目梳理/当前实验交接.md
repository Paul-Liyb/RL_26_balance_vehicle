# 当前实验交接

## 1. 当前项目状态

这个项目目前已经完成了“算法实验层”的第一版搭建，核心目标是围绕现有线性离散平衡环境，做 `LQR + SAC + TD3 + PPO` 的对比，并为后续真车 PC 端控制替换保留接口。

当前真正已经实现并可用的部分包括：

- 控制链路梳理文档
- MATLAB 到 Python 的 LQR 复现脚本
- 基于 Gymnasium 的线性站立环境
- 基于 Stable-Baselines3 的 `SAC/TD3/PPO` 训练入口
- 统一评估与画图脚本
- 沿用原 TCP 协议的 PC 端 live control 入口

## 2. 当前关键文件

- 控制链路说明：
  - `D:\code_wf\Y1S1\强化学习\项目梳理\控制链路说明.md`
- LQR 求解：
  - `D:\code_wf\Y1S1\强化学习\tools\lqr_from_matlab.py`
- 环境定义：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\env.py`
- 训练配置：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
- 训练与评估核心：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\experiments.py`
- 训练入口：
  - `D:\code_wf\Y1S1\强化学习\tools\train.py`
- 全流程入口：
  - `D:\code_wf\Y1S1\强化学习\tools\run_full_experiment.py`
- 当前最佳完整实验输出：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_full_cuda`

## 3. 已验证环境

- WSL：`Ubuntu-24.04`
- Python：`python3`
- PyTorch：`2.11.0+cu130`
- `torch.cuda.is_available() == True`
- GPU：`NVIDIA GeForce RTX 5080`

已重新验证：

- `python3 -m unittest discover -v tests`
- `11/11` 测试通过

## 4. 当前实验结果

来自：

- `D:\code_wf\Y1S1\强化学习\tools\artifacts_full_cuda\summary\algorithm_summary.csv`

聚合结果：

- `lqr`
  - `success_rate = 0.69`
  - `mean_return = 614.08`
  - `mean_control_energy = 9635.35`
- `sac`
  - `success_rate = 0.47`
  - `mean_return = 460.77`
  - `mean_control_energy = 4749.65`
- `ppo`
  - `success_rate = 0.00`
  - `mean_return = 13.17`
  - `mean_control_energy = 222913.87`
- `td3`
  - `success_rate = 0.00`
  - `mean_return = 1.58`
  - `mean_control_energy = 179005.96`

## 5. 当前判断

- 实验代码不是坏的，管线已经能完整跑通。
- `SAC` 是当前唯一有继续优化价值的 RL 方法。
- `PPO` 和 `TD3` 在当前环境和奖励定义下基本失败。
- 当前更像是：
  - reset 分布偏难
  - reward/termination 对学习不够友好
- 不是优先去换算法库的时候。

## 6. 已知注意点

- 中文路径在 PowerShell / WSL 输出里可能会出现乱码，但磁盘上的真实文件没问题。
- `D:\code_wf` 不是 git 仓库根目录。
- 如果改了 reset、termination、horizon 或其他会改变评估难度的设定，必须重新跑 LQR，再比较 RL 是否真的提升。

## 7. 下一轮最推荐的方向

优先建议只选一个方向开始：

1. 把 reset 分布参数化，并先缩小训练初始扰动，再观察 SAC 能否更稳定地学会站立
2. 重新设计 reward 权重与 action penalty，但保持其他条件不变

推荐先做第 1 个。

## 8. 暂时不要优先碰的部分

- `wifi3.0.py`
- 固件通信协议
- 板端控制逻辑
- `live_control.py` 的协议层
- PPO 调参
- TD3 调参

## 9. 产物位置

当前已有图表：

- `D:\code_wf\Y1S1\强化学习\tools\artifacts_full_cuda\plots\training_return_curve.png`
- `D:\code_wf\Y1S1\强化学习\tools\artifacts_full_cuda\plots\success_rate_bar.png`
- `D:\code_wf\Y1S1\强化学习\tools\artifacts_full_cuda\plots\control_energy_bar.png`
- `D:\code_wf\Y1S1\强化学习\tools\artifacts_full_cuda\plots\rollout_timeseries.png`

后续建议新的研究迭代统一写到：

- `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_YYYYMMDD_HHMM_<slug>`

## 10. 2026-04-07 Iteration 1 更新

### 本轮主假设

- 先不改评估协议，只把训练阶段的 reset 分布参数化并缩窄，观察 `SAC` 是否能更稳定地学会站立。

### 本轮实际改动

- 已新增训练专用 reset profile：
  - `default`
  - `narrow`
- 已保持评估环境默认 reset 不变，因此本轮不需要重跑 `LQR`。
- 已修改文件：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\env.py`
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\experiments.py`
  - `D:\code_wf\Y1S1\强化学习\tools\train.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_balance_env.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiment_cli.py`

### 本轮验证与 screening

- 单测：
  - `python3 -m unittest discover -v tests`
  - `13/13` 通过
- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\screen\summary`
- screening 聚合结果：
  - `lqr`: `success_rate = 0.69`, `mean_return = 614.08`, `mean_control_energy = 9635.35`
  - `sac`: `success_rate = 0.295`, `mean_return = 293.60`, `mean_control_energy = 5468.10`
- 分 seed 结果：
  - `seed_0`: `success_rate = 0.27`, `mean_return = 267.09`, `mean_control_energy = 6446.17`
  - `seed_1`: `success_rate = 0.32`, `mean_return = 320.11`, `mean_control_energy = 4490.03`
- 对比现有 `artifacts_full_cuda` 的同 budget `120k` callback：
  - `seed_0`：旧 `success_rate = 0.50`，新 `0.40`
  - `seed_1`：旧 `success_rate = 0.30`，新 `0.30`

### 本轮结论

- 训练专用“统一缩窄 reset”没有带来有价值的提升，不升格 full run。
- 当前更像是：
  - 统一缩窄全部状态维度后，训练分布与默认评估分布出现了不利的鲁棒性错配
  - 简单地“把所有初始扰动都变小”不是当前最优方向
- 本轮不是新 best。

### 下一轮最推荐的单一方向

- 仍然优先尝试 reset 分布，但不要再统一缩窄全部维度。
- 推荐下一轮假设：
  - 只缩窄 `theta_1/theta_2` 及其对应角速度的训练初始扰动，保留轮角和轮速 reset 接近默认分布，观察是否能在不丢鲁棒性的前提下改善早期学习。

## 11. 2026-04-07 Iteration 2 更新

### 本轮主假设

- 第一轮失败不是 reset 方向整体无效，而是“统一缩窄全部状态维度”损伤了轮系相关状态的鲁棒性。
- 因此本轮只缩窄 `theta_1/theta_2` 与对应角速度的训练初始扰动，保留轮角和轮速接近默认分布。

### 本轮实际改动

- 已新增训练专用 reset profile：
  - `posture_focus`
- 已修改文件：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_balance_env.py`

### 本轮验证与 screening

- 单测：
  - `python3 -m unittest discover -v tests`
  - `14/14` 通过
- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_posture_focus_reset\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_posture_focus_reset\screen\summary`
- screening 聚合结果：
  - `lqr`: `success_rate = 0.69`, `mean_return = 614.08`, `mean_control_energy = 9635.35`
  - `sac`: `success_rate = 0.34`, `mean_return = 333.49`, `mean_control_energy = 5706.33`
- 分 seed 结果：
  - `seed_0`: `success_rate = 0.33`, `mean_return = 319.58`, `mean_control_energy = 6485.45`
  - `seed_1`: `success_rate = 0.35`, `mean_return = 347.41`, `mean_control_energy = 4927.20`
- 对比 Iteration 1：
  - `success_rate`: `0.295 -> 0.34`
  - `mean_return`: `293.60 -> 333.49`
  - `rms_theta_1`: `0.03447 -> 0.03117`
  - `rms_theta_2`: `0.05123 -> 0.04573`
  - `mean_control_energy`: `5468.10 -> 5706.33`
- 但对比现有 `artifacts_full_cuda` 的同 budget `120k` callback 仍然不占优：
  - `seed_0`：旧 `success_rate = 0.50`，新 `0.40`
  - `seed_1`：旧 `success_rate = 0.30`，新 `0.25`

### 本轮结论

- `posture_focus` 比第一轮 `narrow` 更好，但仍明显低于当前 best SAC，不升格 full run。
- reset 分布方向已经连续两轮没有跑出足够有价值的 screening 提升。
- 当前最合理的下一个方向应转向 reward 权重，而不是继续在 reset 上小修小补。

### 下一轮最推荐的单一方向

- 保持 reset 使用默认评估分布相关设定不动。
- 推荐下一轮假设：
  - 重设计 reward 权重，只进一步强调 `theta_1/theta_2` 稳定并下调轮系项权重，先不同时改 SAC 超参；如果采用新 reward，需要重算当前协议下的 `LQR`/baseline summary 再比较。

## 12. 2026-04-07 Iteration 3 更新与停表诊断

### 本轮主假设

- 当前默认 reward 对轮系项分散了过多关注，训练目标没有足够聚焦 `theta_1/theta_2` 姿态稳定。
- 因此本轮只改训练阶段 reward profile，评估仍使用默认 reward/默认环境，观察 `SAC` 是否能学到更接近站立目标的策略。

### 本轮实际改动

- 已新增训练专用 reward profile：
  - `posture_focus`
- 已修改文件：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\env.py`
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\experiments.py`
  - `D:\code_wf\Y1S1\强化学习\tools\train.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_balance_env.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiment_cli.py`
- 单测：
  - `python3 -m unittest discover -v tests`
  - `16/16` 通过

### 本轮 screening

- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus\screen\summary`
- screening 聚合结果：
  - `lqr`: `success_rate = 0.69`, `mean_return = 614.08`, `mean_control_energy = 9635.35`
  - `sac`: `success_rate = 0.445`, `mean_return = 433.41`, `mean_episode_length = 469.55`, `rms_theta_1 = 0.02334`, `rms_theta_2 = 0.04628`, `mean_control_energy = 4171.94`
- 分 seed 结果：
  - `seed_0`: `success_rate = 0.40`, `mean_return = 388.48`, `mean_control_energy = 4861.71`
  - `seed_1`: `success_rate = 0.49`, `mean_return = 478.35`, `mean_control_energy = 3482.16`

### 与前三组基线的关系

- 对比 Iteration 1 `narrow`：
  - `success_rate`: `0.295 -> 0.445`
  - `mean_return`: `293.60 -> 433.41`
  - `mean_control_energy`: `5468.10 -> 4171.94`
- 对比 Iteration 2 `posture_focus reset`：
  - `success_rate`: `0.34 -> 0.445`
  - `mean_return`: `333.49 -> 433.41`
  - `mean_control_energy`: `5706.33 -> 4171.94`
- 对比当前 best SAC（完整 300k / 5 seeds）：
  - `success_rate`: `0.47` vs `0.445`
  - `mean_return`: `460.77` vs `433.41`
  - `mean_episode_length`: `490.80` vs `469.55`
  - `rms_theta_1`: 本轮略优
  - `rms_theta_2`: 本轮仍略差
  - `mean_control_energy`: 本轮更低
- 对比同 budget `120k` callback（`artifacts_full_cuda` 的 `seed_0/1`）：
  - `seed_0`: `success_rate` 持平 `0.50`，但 `mean_return` 更高、`mean_control_energy` 更低
  - `seed_1`: `success_rate` 从 `0.30 -> 0.40`，`mean_return` 更高、`mean_control_energy` 更低

### 停表结论

- 这三轮 screening 中，只有 Iteration 3 的 reward shaping 显示出明确价值，但它仍未在 screening 门槛上超过当前 best SAC，因此按流程不升格 full run。
- 至此已经出现“连续 3 轮 screening 未优于当前 best SAC”的情况，停止继续盲试，转入诊断结论。

### 当前可信诊断

- 当前瓶颈不像是“只要再训练更久”。
  - 现有 `300k` baseline 的末段大多是平台期或波动，不是稳定单调上升。
- reset 分布方向不是零价值，但“统一缩窄”会显著伤害默认评估鲁棒性。
- 比起 reset，小幅 reward shaping 更有效。
  - 它在同 budget 下已经比默认训练更接近当前 best SAC，并且控制能量更低。
- 因此后续最有价值的两个方向应是：
  1. 以 `posture_focus reward` 为基础，再尝试 `SAC` 超参数而不是回到 reset 微调。
  2. 仅在 `posture_focus reward` 基础上，再验证更长训练预算是否能真正越过当前 best SAC，再决定要不要挑战 LQR。

## 13. 2026-04-07 Iteration 4 更新

### 本轮主假设

- 在 Iteration 3 的 `posture_focus reward` 基础上，当前 120k screening 的主要瓶颈更像是样本效率。
- 因此本轮只改一个 `SAC` 超参：把 `batch_size` 从 `256` 降到 `128`，观察是否能把 Iteration 3 末段还在上升的收益提前释放出来。

### 本轮实际改动

- 已新增训练专用 `SAC` profile：
  - `batch128`
- 已修改文件：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\experiments.py`
  - `D:\code_wf\Y1S1\强化学习\tools\train.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiments.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiment_cli.py`
- 单测：
  - `python3 -m unittest discover -v tests`
  - `18/18` 通过

### 本轮 screening

- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus_sac_batch128\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus_sac_batch128\screen\summary`
- screening 聚合结果：
  - `lqr`: `success_rate = 0.69`, `mean_return = 614.08`, `mean_control_energy = 9635.35`
  - `sac`: `success_rate = 0.455`, `mean_return = 441.01`, `mean_episode_length = 478.82`, `rms_theta_1 = 0.02586`, `rms_theta_2 = 0.03984`, `mean_control_energy = 4289.82`
- 分 seed 结果：
  - `seed_0`: `success_rate = 0.41`, `mean_return = 399.40`, `mean_control_energy = 5249.74`
  - `seed_1`: `success_rate = 0.50`, `mean_return = 482.63`, `mean_control_energy = 3329.90`

### 与 Iteration 3 / 当前 best SAC 对比

- 对比 Iteration 3：
  - `success_rate`: `0.445 -> 0.455`
  - `mean_return`: `433.41 -> 441.01`
  - `mean_episode_length`: `469.55 -> 478.82`
  - `rms_theta_2`: `0.04628 -> 0.03984`
  - `mean_control_energy`: `4171.94 -> 4289.82`
- 对比当前 best SAC：
  - `success_rate`: `0.455` vs `0.47`
  - `mean_return`: `441.01` vs `460.77`
  - `mean_episode_length`: `478.82` vs `490.80`
  - `rms_theta_2`: 本轮略优
  - `mean_control_energy`: 本轮更低

### 本轮结论

- `batch128` 带来了小幅正向提升，但仍未超过当前 best SAC，不升格 full run。
- 这说明：
  - 方向是对的
  - 但仅靠 120k 预算下的单超参改动，还不足以完成反超
- 在当前最优训练配置已经接近 current best SAC 的前提下，下一轮最值得检验的单一假设就是“更久训练是否真的有用”。

### 下一轮最推荐的单一方向

- 保持 `posture_focus reward + SAC batch128` 不变。
- 只增加训练 timesteps 做新的 screening，验证在改进后的设定下，延长训练是否能把 aggregate `success_rate` 推过当前 best SAC。

## 14. 2026-04-08 Iteration 5 更新

### 本轮主假设

- 在当前最优训练配置 `posture_focus reward + SAC batch128` 下，之前 120k screening 仍在末段上升。
- 因此本轮只增加训练预算，先用 `200k` screening 验证“更久训练”在改进后的设定下是否成立；若 screening 超过当前 best SAC，再升格 full run。

### 本轮验证与筛选

- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus_sac_batch128_200k\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus_sac_batch128_200k\screen\summary`
- 200k screening 聚合结果：
  - `sac`: `success_rate = 0.49`, `mean_return = 477.23`, `mean_episode_length = 512.16`, `rms_theta_1 = 0.02410`, `rms_theta_2 = 0.04370`, `mean_control_energy = 4641.59`
- 相比当前 best SAC：
  - `success_rate`: `0.47 -> 0.49`
  - `mean_return`: `460.77 -> 477.23`
  - `mean_episode_length`: `490.80 -> 512.16`
  - `mean_control_energy`: `4749.65 -> 4641.59`
- 结论：
  - screening 已满足升格条件，因此执行 full run。

### 本轮 full run

- full run 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus_sac_batch128_200k\full`
- full run 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus_sac_batch128_200k\full\summary`
- full run 图表目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260407_reward_posture_focus_sac_batch128_200k\full\plots`
- full run 聚合结果：
  - `lqr`: `success_rate = 0.69`, `mean_return = 614.08`, `mean_episode_length = 690.31`, `rms_theta_1 = 0.01480`, `rms_theta_2 = 0.00733`, `mean_control_energy = 9635.35`
  - `sac`: `success_rate = 0.516`, `mean_return = 494.70`, `mean_episode_length = 534.58`, `rms_theta_1 = 0.02461`, `rms_theta_2 = 0.03619`, `mean_control_energy = 4521.46`

### 与基线对比

- 对比旧 best SAC：
  - `success_rate`: `0.47 -> 0.516`
  - `mean_return`: `460.77 -> 494.70`
  - `mean_episode_length`: `490.80 -> 534.58`
  - `mean_control_energy`: `4749.65 -> 4521.46`
- 对比当前 LQR：
  - `success_rate`: `0.516 < 0.69`
  - `mean_return`: `494.70 < 614.08`
  - `mean_episode_length`: `534.58 < 690.31`
  - `mean_control_energy`: 更低，但 `rms_theta_1/rms_theta_2` 仍明显差于 LQR

### 本轮结论

- 这一轮已经稳定超过旧 best SAC，当前新的 best RL 配置为：
  - 训练 reward：`posture_focus`
  - `SAC` profile：`batch128`
  - 更长训练预算有效
- 但它仍未达到主目标：尚未超过当前 LQR。
- 从 full run 各 seed 看：
  - `seed_0/3/4` 具备较强站立能力
  - `seed_2` 明显拖后腿，说明当前主要问题开始从“学不会”转向“跨 seed 稳定性不足”

### 下一轮最推荐的单一方向

- 保持 `posture_focus reward + batch128` 不变。
- 推荐下一轮只动一个稳定性相关超参，优先考虑降低 `learning_rate`，观察是否能抬高最差 seed、进一步提高 5-seed 聚合成功率。

## 15. 2026-04-08 Iteration 6 更新

### 本轮主假设

- 当前最强配置的主要问题是跨 seed 稳定性。
- 因此本轮只降低一个稳定性相关超参：把 `learning_rate` 从 `3e-4` 降到 `1e-4`，观察是否能抬高最差 seed。

### 本轮实际改动

- 已新增训练专用 `SAC` profile：
  - `batch128_lr1e4`
- 已修改文件：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiments.py`
- 单测：
  - `python3 -m unittest discover -v tests`
  - `19/19` 通过

### 本轮 screening

- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_reward_posture_focus_sac_batch128_lr1e4_200k\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_reward_posture_focus_sac_batch128_lr1e4_200k\screen\summary`
- screening 聚合结果：
  - `sac`: `success_rate = 0.26`, `mean_return = 281.81`, `mean_episode_length = 345.57`, `rms_theta_1 = 0.04501`, `rms_theta_2 = 0.05854`, `mean_control_energy = 4538.13`
- 分 seed 结果：
  - `seed_0`: `success_rate = 0.10`, `mean_return = 157.71`, `mean_control_energy = 6129.86`
  - `seed_1`: `success_rate = 0.42`, `mean_return = 405.91`, `mean_control_energy = 2946.40`

### 与当前 best RL 的对比

- 对比 Iteration 5 screening：
  - `success_rate`: `0.49 -> 0.26`
  - `mean_return`: `477.23 -> 281.81`
  - `mean_episode_length`: `512.16 -> 345.57`
  - `rms_theta_1/rms_theta_2` 明显恶化
- 对比 Iteration 5 full run：
  - 明显更差，不具备 full run 价值

### 本轮结论

- `learning_rate = 1e-4` 对当前任务是明显负向改动，不升格 full run。
- 诊断：
  - 这套配置当前更需要的是“更好的长期目标权衡”而不是“更慢的优化速度”
  - 过低学习率会让 200k 预算内策略仍然处于明显欠学状态

### 下一轮最推荐的单一方向

- 回到当前 best 配置：
  - `posture_focus reward`
  - `SAC batch128`
- 推荐下一轮只动 `gamma`，提高长期回报权重，观察是否能进一步抬升 `mean_episode_length` 和 `success_rate`。

## 16. 2026-04-08 Iteration 7 更新与阶段性停表诊断

### 本轮主假设

- 当前 best RL 结果的短板仍然是长期稳定性不足。
- 因此本轮回到当前 best 配置，只提高 `gamma`：`0.995 -> 0.998`，观察是否能进一步抬高 `mean_episode_length` 与 `success_rate`。

### 本轮实际改动

- 已新增训练专用 `SAC` profile：
  - `batch128_gamma998`
- 已修改文件：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiments.py`
- 单测：
  - `python3 -m unittest discover -v tests`
  - `20/20` 通过

### 本轮 screening

- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_reward_posture_focus_sac_batch128_gamma998_200k\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_reward_posture_focus_sac_batch128_gamma998_200k\screen\summary`
- screening 聚合结果：
  - `sac`: `success_rate = 0.555`, `mean_return = 514.51`, `mean_episode_length = 571.49`, `rms_theta_1 = 0.02212`, `rms_theta_2 = 0.03331`, `mean_control_energy = 4209.53`
- 结论：
  - screening 明显优于当前 best RL，升格 full run。

### 本轮 full run

- full run 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_reward_posture_focus_sac_batch128_gamma998_200k\full`
- full run 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_reward_posture_focus_sac_batch128_gamma998_200k\full\summary`
- full run 图表目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_reward_posture_focus_sac_batch128_gamma998_200k\full\plots`
- full run 聚合结果：
  - `lqr`: `success_rate = 0.69`, `mean_return = 614.08`, `mean_episode_length = 690.31`, `rms_theta_1 = 0.01480`, `rms_theta_2 = 0.00733`, `mean_control_energy = 9635.35`
  - `sac`: `success_rate = 0.528`, `mean_return = 509.73`, `mean_episode_length = 544.98`, `rms_theta_1 = 0.02119`, `rms_theta_2 = 0.03553`, `mean_control_energy = 4253.00`

### 与旧 best RL / LQR 的对比

- 对比 Iteration 5 full run：
  - `success_rate`: `0.516 -> 0.528`
  - `mean_return`: `494.70 -> 509.73`
  - `mean_episode_length`: `534.58 -> 544.98`
  - `rms_theta_1`: `0.02461 -> 0.02119`
  - `rms_theta_2`: `0.03619 -> 0.03553`
  - `mean_control_energy`: `4521.46 -> 4253.00`
- 对比 LQR：
  - `success_rate`: `0.528 < 0.69`
  - `mean_return`: `509.73 < 614.08`
  - `mean_episode_length`: `544.98 < 690.31`
  - `rms_theta_1/rms_theta_2` 仍显著差于 LQR
  - `mean_control_energy` 低于 LQR，但这不足以弥补稳定性差距

### 阶段性结论

- 当前新的 best RL 配置为：
  - 训练 reward：`posture_focus`
  - `SAC` profile：`batch128_gamma998`
- 它稳定超过了旧 best SAC，但仍未在当前评估协议下超过 LQR。
- 同时，当前已超过“连续 6 轮仍未超过 LQR”的停表条件。
- 结合 `gamma998` full run 末段表现：
  - 部分 seed 仍有提升，但整体并不呈现一致的持续上升
  - 问题已经不再像“多训一点就自然过线”，而更像跨 seed 鲁棒性和目标错配仍未解决

### 当前最可信的失败诊断

- 当前瓶颈更像是：
  - 奖励目标与“长期站立成功”仍有残余错配
  - `SAC` 在这个环境上的跨 seed 方差仍偏大
  - 单纯继续拉训练预算的收益不再稳定
- 已验证结论：
  - reset 微调收益有限，且容易伤害默认评估鲁棒性
  - reward shaping 明显有效
  - `batch_size=128` 有效
  - `gamma=0.998` 继续有效
  - `learning_rate=1e-4` 明显无效
- 若要继续，最值得尝试的两个方向是：
  1. 保持当前 best 配置，只重新设计 reward 里的终止失败惩罚或成功导向项，而不是再做一般性 reset 微调。
  2. 保持当前 best 配置，只动一个与稳定性相关的 `SAC` 超参，例如 target update / replay 相关参数，而不是继续降低学习率。

## 17. 2026-04-08 Iteration 8 更新

### 本轮主假设

- 纯 `SAC` 小修小补已经接近瓶颈，下一条最有机会超过强 `LQR` 的路线是让 `RL` 学残差控制，而不是从零输出全部动作。
- 因此本轮保持当前 best 配置不变，只引入残差动作结构：
  - `u = clip(u_lqr + 0.15 * u_residual, -1, 1)`

### 本轮实际改动

- 已新增训练专用 action mode：
  - `residual_lqr`
- 已修改文件：
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\config.py`
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\env.py`
  - `D:\code_wf\Y1S1\强化学习\tools\rl_balance\experiments.py`
  - `D:\code_wf\Y1S1\强化学习\tools\train.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_balance_env.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiments.py`
  - `D:\code_wf\Y1S1\强化学习\tools\tests\test_experiment_cli.py`
- 单测：
  - `python3 -m unittest discover -v tests`
  - `23/23` 通过

### 本轮 screening

- screening 训练输出目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_residual_lqr_sac_batch128_gamma998_scale015\screen`
- screening 汇总目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_residual_lqr_sac_batch128_gamma998_scale015\screen\summary`
- screening 图表目录：
  - `D:\code_wf\Y1S1\强化学习\tools\artifacts_research\iter_20260408_residual_lqr_sac_batch128_gamma998_scale015\screen\plots`
- 本轮训练配置：
  - reward：`posture_focus`
  - `SAC` profile：`batch128_gamma998`
  - action mode：`residual_lqr`
  - residual scale：`0.15`
  - screening budget：`2 seeds`, `200k`
- screening 聚合结果：
  - `lqr`: `success_rate = 0.69`, `mean_return = 614.08`, `mean_episode_length = 690.31`, `mean_control_energy = 9635.35`
  - `sac`: `success_rate = 0.57`, `mean_return = 432.59`, `mean_episode_length = 571.22`, `rms_theta_1 = 0.02971`, `rms_theta_2 = 0.00912`, `mean_control_energy = 165662.35`
- 分 seed 结果：
  - `seed_0`: `success_rate = 0.45`, `mean_return = 340.48`, `mean_episode_length = 451.95`, `mean_control_energy = 130133.19`
  - `seed_1`: `success_rate = 0.69`, `mean_return = 524.70`, `mean_episode_length = 690.49`, `mean_control_energy = 201191.50`

### 与当前 best RL / LQR 的关系

- 对比当前 best RL full run（`gamma998`）：
  - `success_rate`: `0.528 -> 0.57`
  - `mean_episode_length`: `544.98 -> 571.22`
  - `rms_theta_2`: 明显更好
  - 但 `mean_return`: `509.73 -> 432.59`
  - 且 `mean_control_energy`: `4253.00 -> 165662.35`，出现数量级恶化
- 对比当前 LQR：
  - `success_rate`: 仍低于 `0.69`
  - `mean_episode_length`: 已接近 `690.31`
  - `mean_control_energy`: 远高于 `LQR`

### 本轮结论

- 残差动作结构是有价值的：
  - 它是第一次在当前停表后的新主线上，把 screening `success_rate` 推到 `0.57`
  - 说明“让 RL 站在 LQR 肩膀上”这条方向是对的
- 但当前这版 `residual_scale = 0.15` 存在明显新问题：
  - 长时成功率提升的同时，控制能量极大
  - 这更像策略学会了“用很激进的残差去保住站立”，而不是优雅地小幅补偿 `LQR`
- 因此本轮不直接升格 `5-seed / 300k` full run。
  - 仅按 `success_rate` 看，它已经具备升格信号
  - 但若忽略当前能量爆炸直接晋级，会把新的变量风险带进 full run，解释成本太高

### 当前最可信的新诊断

- 这条 residual 路线的主要风险不再是“学不会”，而是：
  - residual 幅度过大后，策略会走向高能量补偿
  - 当前 reward 虽然仍能鼓励站立，但对这种 residual 激进控制的抑制不够
- 换句话说：
  - `Residual RL` 方向本身是对的
  - 但第一版需要先把 residual 使用方式收敛得更克制

### 下一轮最推荐的单一方向

- 保持当前 residual 结构与其余配置不变：
  - `posture_focus reward`
  - `SAC batch128_gamma998`
  - `action_mode = residual_lqr`
- 只改一个变量：
  - 把 `residual_scale` 从 `0.15` 下调到更保守值，例如 `0.10`
- 目标不是继续冲更高 reward，而是验证：
  - 能否保留大部分 `success_rate / mean_episode_length` 提升
  - 同时把 `mean_control_energy` 拉回到更合理的量级
