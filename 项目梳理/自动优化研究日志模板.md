# 自动优化研究日志模板

每一轮优化结束后，追加一段新记录，不要覆盖旧记录。

---

## Iteration <编号>

### 1. 时间

- 开始时间：
- 结束时间：

### 2. 本轮主假设

- 假设：
- 为什么优先试它：

### 3. 改动范围

- 修改文件：
- 未修改但重新验证的文件或产物：

### 4. 执行命令

```bash
# 在这里粘贴实际执行过的命令
```

### 5. 结果

- screening 是否完成：
- screening 输出目录：
- screening `success_rate`：
- screening `mean_return`：
- screening `mean_control_energy`：
- 是否晋级 full run：

### 6. 如果执行了 full run

- full run 输出目录：
- full run `success_rate`：
- full run `mean_return`：
- full run `mean_episode_length`：
- full run `rms_theta_1`：
- full run `rms_theta_2`：
- full run `mean_control_energy`：

### 7. 与基线对比

- 当前 LQR：
- 上一 best SAC：
- 本轮结果是否优于上一 best SAC：
- 本轮结果是否超过当前 LQR：

### 8. 结论

- 本轮是否成为新 best：
- 如果没有，主要失败原因是什么：
- 下一轮最值得尝试的单一假设是什么：

### 9. 备注

- 依赖问题：
- 运行异常：
- 对后续实验的提醒：
